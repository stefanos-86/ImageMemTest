# Fails because the image is 256 pixel, so the centre has to be at least 128 pixels on the right.
# Remember that the origin is the top left corner and coordinates increase towards the right and bottom.
ShowImage(1000, 122, 320, "A.jpg")